# ============================================
# IPSSI_PATCH Backend Environment Variables
# ============================================
#
# SECURITY NOTICE:
# - NEVER commit the .env file to git (it's already in .gitignore)
# - Use strong passwords in production (min 16 characters, alphanumeric + symbols)
# - Change all default values before deploying to production
# - Keep this file as a template, actual values in .env
#
# ============================================

# Server Configuration
PORT=8000
NODE_ENV=development  # Use 'production' for production environment

# Database Configuration
# For Docker: use 'postgres' as DB_HOST
# For local dev: use 'localhost' as DB_HOST
DB_HOST=postgres
DB_PORT=5432
DB_NAME=ipssi_patch
DB_USER=admin
# SECURITY: Change this password in production!
# Recommendation: Use a strong password (min 16 chars, alphanumeric + symbols)
DB_PASSWORD=securepassword123

# CORS Configuration
# Allowed frontend origin for CORS
# Production: Use your actual frontend URL (e.g., https://yourdomain.com)
FRONTEND_URL=http://localhost:3000

# ============================================
# Production Security Checklist:
# ============================================
# [ ] Change DB_PASSWORD to a strong password
# [ ] Set NODE_ENV=production
# [ ] Update FRONTEND_URL to production URL
# [ ] Use HTTPS for FRONTEND_URL
# [ ] Review all security settings
# ============================================
